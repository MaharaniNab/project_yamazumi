[phases.setup]
nixPkgs = ["python311", "libGL", "glib"]

[phases.install]
cmds = ["pip install -r api/requirements.txt"]

[start]
cmd = "gunicorn --chdir api index:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300"
